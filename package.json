{
  "name": "tile-generator",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "add": "lerna add",
    "bootstrap": "lerna bootstrap",
    "postinstall": "npm run bootstrap",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "pub": "lerna publish"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
