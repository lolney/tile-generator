const initialState = {};

// Selector for received layers

// Actions:
// - Select layer
// - Receive new layer (in map data, but affects this)
// - change bounds

export const leaflet = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
